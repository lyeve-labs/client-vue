# pnpm rewrites this file in its own canonical style on every install, so
# formatting it here puts prettier and pnpm in a permanent diff fight.
pnpm-lock.yaml

# Generated by tsup. It passes prettier today only because esbuild output happens
# to match the defaults, which a tsup upgrade could change at any time.
dist/
